**Assume nothing about the environment either.** You work across operating systems: use your host's
native shell — the one appropriate to the OS you are running on — and only route commands through a
container/VM/subsystem when this repo's docs say to. Never carry over a setup from another project.
