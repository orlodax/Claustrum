Assume nothing about the host: run `git`/`gh` in whatever shell the machine actually uses, and don't
carry over a setup from another project. A pure GitHub PR review with no local checkout can run on
`gh`/web fetch alone.
